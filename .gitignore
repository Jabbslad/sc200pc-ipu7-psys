# User-supplied OEM/Intel artifacts (verified by hash, never committed)
/artifacts/

# Kernel build products
*.ko
*.ko.zst
*.o
*.mod
*.mod.c
*.symvers
*.order
.*.cmd
Module.symvers
modules.order

# Editor / OS
*.swp
.DS_Store

# Downloaded OEM package (scripts/download-windows-driver.sh) -- proprietary, never committed
/downloads/
# HAL source clone + build tree (scripts/build-hal-plugin.sh)
/build/
